<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uc.finalproject">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logosa1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.splash_screen.SplashActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:parentActivityName=".ui.splash_screen.SplashActivity" />
        <activity
            android:name=".ui.notes.NotesAddActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".ui.notes.NotesResult"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".ui.notes.NotesDelete" android:parentActivityName=".MainActivity"/>
    </application>

</manifest>